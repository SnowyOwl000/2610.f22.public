#include <iostream>

using namespace std;

int main() {
  // variables

  // area = 0

  // get first point

  // copy first point to current point

  // do {

    // copy current point to previous point

    // get current point

    // update area: area = area + prev x * cur y - prev y * cur x

  // } while current point != previous point

  // area = area / 2

  // output area

  return 0;
}

